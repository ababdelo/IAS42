#!/bin/sh
set -e

echo "[IAS42] MySQL init scripts directory mounted successfully."
echo "[IAS42] Any .sql or .sh file in /docker-entrypoint-initdb.d will run automatically on first database initialization."
